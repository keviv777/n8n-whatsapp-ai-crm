# N8N Workflows

This directory will contain the exported `n8n` workflow JSON strings needed to replicate the CRM.

1. **WhatsApp Cold Outreach.json**
2. **AI Reply Classification.json**

*To use them: open n8n → Workflows → Create New → Import from File.*
